<nav class="hidden md:flex flex-col w-64 bg-white border-r border-gray-200 px-4 py-6 fixed inset-y-0 left-0 z-40 md:static md:z-auto" data-mobile-menu-target="menu">
  <div class="mb-8 flex items-center justify-between">
    <h1 class="text-xl font-bold text-gray-900 px-3">Uncal</h1>
    <button type="button" data-action="mobile-menu#close" class="text-gray-600 hover:text-gray-900 md:hidden">
      <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </div>

  <div class="flex-1 space-y-1">
    <%= sidebar_link_to "Dashboard", root_path, icon: "home" %>
    <%= sidebar_link_to "Schedule Links", schedule_links_path, icon: "calendar" %>
    <%= sidebar_link_to "Workflows", workflows_path, icon: "mail" %>
    <%= sidebar_link_to "Bookings", admin_bookings_path, icon: "clock" %>
    <%= sidebar_link_to "Contacts", contacts_path, icon: "users" %>
  </div>

  <div class="mt-auto space-y-1 border-t border-gray-200 pt-4">
    <%= sidebar_link_to "Settings", edit_settings_path, icon: "settings" %>
    <%= button_to "Sign out", session_path, method: :delete, class: "flex items-center w-full gap-3 rounded-lg px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-100 hover:text-gray-900" %>
  </div>
</nav>
